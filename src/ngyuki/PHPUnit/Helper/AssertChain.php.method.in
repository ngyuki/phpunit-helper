    {%doc}
    public function {%name}({%args})
    {
        Assert::assertThat($this->val, call_user_func_array(
            '{%class}::{%name}', func_get_args())
        );
        return $this;
    }
